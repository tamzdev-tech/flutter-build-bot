name: Build Flutter APK

on:
  repository_dispatch:
    types: [flutter-build-request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0' # Sesuaikan versi flutter Anda jika perlu
          channel: 'stable'

      - name: Get Dependencies
        run: flutter pub get

      - name: Build APK (Debug/Release)
        run: |
          # Perintah ini akan mendeteksi otomatis build mode dari payload bot
          flutter build apk --release
